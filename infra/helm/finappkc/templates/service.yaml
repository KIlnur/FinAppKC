apiVersion: v1
kind: Service
metadata:
  name: {{ include "finappkc.fullname" . }}
  labels:
    {{- include "finappkc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finappkc.selectorLabels" . | nindent 4 }}
